interface:
  display_name: "PostPlan"
  short_description: "Publish an existing static HTML document"
  default_prompt: "Use $postplan to publish this static HTML document and return its public URL."
