interface:
  display_name: "Nix Dependencies"
  short_description: "Run helpers with temporary Nix dependencies"
  default_prompt: "Use $nix-dependencies to select a complete temporary runtime and run this standalone helper."
