# Multi-phase plan

Use `update_plan` with independently verifiable phases, explicit dependencies, safe stopping points, and the final end-to-end proof. Do not create a plan file unless the user requested one.
